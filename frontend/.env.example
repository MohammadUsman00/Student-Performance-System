# Copy to `frontend/.env.local` (Vite does not commit secrets there).
#
# Match project root `.env.local` → `CONVEX_URL`:
#   - Local backend: http://127.0.0.1:3210
#   - Cloud/dev deployment: https://YOUR_NAME.convex.cloud

VITE_CONVEX_URL=http://127.0.0.1:3210
